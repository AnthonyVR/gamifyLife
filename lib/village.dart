import 'package:flutter/material.dart';

class Village extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
      body: Center(
        child: Text('Welcome to the Village!'),
      ),
    );
  }
}
